- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item GroupRetentionsRetentionConcept.model_name.human, group_retentions_retention_concepts_path , :active => true

= link_to t('scaffold.actions.show').titlecase, group_retentions_retention_concept_path(@group_retentions_retention_concept), :class => "btn info" 
.clear
%br/
= render 'form'
