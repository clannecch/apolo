- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item GroupRetentionsRetentionConcept.model_name.human, group_retentions_retention_concepts_path
    - breadcrumb.item t('scaffold.actions.new', :item => GroupRetentionsRetentionConcept.model_name.human).titlecase , nil, :active => true

= render 'form'
