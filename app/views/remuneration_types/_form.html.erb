<%= form_for(@remuneration_type) do |f| %>
  <% if @remuneration_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@remuneration_type.errors.count, "error") %> prohibited this remuneration_type from being saved:</h2>

      <ul>
      <% @remuneration_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :detalle %><br />
    <%= f.text_field :detalle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
