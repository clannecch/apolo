- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item SchemaMigration.model_name.human, schema_migrations_path
    - breadcrumb.item t('scaffold.actions.new', :item => SchemaMigration.model_name.human).titlecase , schema_migrations_path, :active => true

= render 'form'
