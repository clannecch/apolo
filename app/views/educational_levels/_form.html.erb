<%= form_for @educational_level do |f| %>
	<% if @educational_level.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@educational_level.errors.count, "error") %> prohibido guardar Nivel de Educacion:</h2>
			<ul>
			<% @educational_level.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>
	<table class="data" style="width:100%" >
		<tr>
			<td style="width:30%;background-color: #C3D5D5;"><strong><%= f.label :detalle %></strong></td>
			<td style="width:70%">
				<%= f.text_field :detalle %>
			</td>
		</tr>
	</table>
	<p><%= f.submit %></p>
<% end %>
