= form_for(@section) do |f|
  = render 'shared/errorhandler', :obj_errors => @section.errors
  .clear
  %br/
  .form_box
    .form_box_title
    /  %span.title= Section.model_name.human
    %table.tableForm
      %tr
        %td.columnLabel
          = f.label :detalle, :class => "apsLabel"
        %td.columnData
          = f.text_field :detalle, :class => "apsTextbox"
      %tr
        %td.columnLabel
          = f.label :code, :class => "apsLabel"
        %td.columnData
          = f.text_field :code, :class => "apsTextbox"
  .actions
    = f.submit :class => "btn success"
