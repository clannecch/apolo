<h1>Listado de secciones</h1>

<table>
  <tr>
    <th>Detalle</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= section.detalle %></td>
    <td><%= link_to 'Visualizar', section %></td>
    <td><%= link_to 'Editar', edit_section_path(section) %></td>
    <td><%= link_to 'Borrar', section, :confirm => 'Esta Seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Seccion', new_section_path %>
