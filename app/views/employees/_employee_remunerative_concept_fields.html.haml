%tr.nested-fields
  %td= f.collection_select :remunerative_concept_id, RemunerativeConcept.all, :id, :detalle, :prompt => true
  %td= f.text_field :detalle
  %td= f.collection_select :cost_center_id, CostCenter.all, :id, :detalle, :prompt => true
  %td= f.text_field :cantidad
  %td= f.text_field :importe
  %td= link_to_remove_association "Borrar", f
