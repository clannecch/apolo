- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item Issued.model_name.human, issueds_path 
    - breadcrumb.item "#{@issued.detalle.titlecase}", issued_path(@issued), :active => true

= link_to t('scaffold.actions.show').titlecase, issued_path(@issued), :class => "btn info" 
.clear
%br/
= render 'form'
