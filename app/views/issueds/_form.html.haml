= form_for(@issued) do |f|
  - if @issued.errors.any?
    #error_explanation
      %h2
        = pluralize(@issued.errors.count, "error")
        prohibited this issued from being saved:
      %ul
        - @issued.errors.full_messages.each do |msg|
          %li= msg
  .clear
  %br/
  .form_box
    .form_box_title
      %span.title= Issued.model_name.human
    %table.tableForm
      %tr
        %td.columnLabel
          = f.label :detalle, :class => "apsLabel"
        %td.columnData
          = f.text_field :detalle, :class => "apsTextbox"
  .actions
    = f.submit :class => "btn success"
