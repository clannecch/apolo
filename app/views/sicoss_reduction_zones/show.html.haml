

- content_for(:title) do
  %h1= [t('scaffold.actions.show'), SicossReductionZone.model_name.human].join(' ').titlecase

- content_for(:controls) do
  = controls do |control|
    - control.item t('scaffold.actions.edit').titlecase, edit_sicoss_reduction_zone_path(@sicoss_reduction_zone), :icon => 'application_edit'
    - control.item t('scaffold.actions.back').titlecase, :back, :icon => 'application_view_list'

= semantic_attributes_for(@sicoss_reduction_zone) do |s|
  = s.attributes(SicossReductionZone.human_name + ' Info') do

    = s.attribute :code

    = s.attribute :name


.clear