
- content_for(:title) do
  %h1= t('scaffold.actions.new', :item => SicossReductionZone.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.back'), sicoss_reduction_zones_path

