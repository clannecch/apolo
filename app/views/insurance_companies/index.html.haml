- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item InsuranceCompany.model_name.human, insurance_companies_path, :active => true
.clear
- if true
  = link_to t('scaffold.actions.new', :item => InsuranceCompany.model_name.human).titlecase , new_insurance_company_path(@insurance_company), :class => "btn success" 
  - if @insurance_companies.any?
    = link_to t('scaffold.actions.export').titlecase, insurance_companies_path( :format => 'pdf'), :class => "btn info"

- if @insurance_companies.any?
  %table.tableStandard
    %tr
      %th= sort_link @search, :detalle, @insurance_companies.human_attribute_name('detalle')
      %th.last

    - @insurance_companies.each do |item|
      %tr
        %td= item.detalle
        %td.last.actions
          = link_to icon('application_get', :small, :title => t('scaffold.actions.show').titlecase), item
          = link_to icon('application_edit', :small, :title => t('scaffold.actions.edit').titlecase), edit_insurance_company_path(item)
          = link_to icon('application_delete', :small, :title => t('scaffold.actions.destroy').titlecase), item, :confirm => t('scaffold.actions.destroy_confirm'), :method => :delete
    .clear
    .block{:style => 'margin:20px;'}= paginate(@insurance_companies)
  .clear
.clear
