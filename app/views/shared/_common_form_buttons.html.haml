= link_to_function(icon('disk') + 'Guardar', "$(this).closest('form').submit();", :class => "btn success")
= link_to_function(icon('arrow_rotate_clockwise') + 'Limpiar', "$(this).closest('form').reset();", :class => "btn standard")
.clear

