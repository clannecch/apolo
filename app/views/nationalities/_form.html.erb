<%= form_for(@nationality) do |f| %>
  <% if @nationality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nationality.errors.count, "error") %> prohibited this nationality from being saved:</h2>

      <ul>
      <% @nationality.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :detalle %><br />
    <%= f.text_field :detalle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
