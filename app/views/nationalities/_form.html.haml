= form_for(@nationality) do |f|
  - if @nationality.errors.any?
    #error_explanation
      %h2
        = pluralize(@nationality.errors.count, "error")
        prohibited this nationality from being saved:
      %ul
        - @nationality.errors.full_messages.each do |msg|
          %li= msg
  %table.tableForm
    %tr
      %td.columnLabel
        = f.label :detalle, :class => "apsLabel"
      %td.columnData
        = f.text_field :detalle, :class => "apsTextbox"
  .actions
    = f.submit :class => "btn success"
