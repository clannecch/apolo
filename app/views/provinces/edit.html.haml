- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item Province.model_name.human, provinces_path 
    - breadcrumb.item "#{@province.detalle.titlecase}", province_path(@province), :active => true

= link_to t('scaffold.actions.show').titlecase, province_path(@province), :class => "btn info" 
.clear
%br/
= render 'form'
