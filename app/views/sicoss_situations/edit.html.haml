
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossSituation.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_situation
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_situations_path

