<%= form_for @accounting_imputation do |f| %>
	<% if @accounting_imputation.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@accounting_imputation.errors.count, "error") %> prohibido guardar Cuenta contable:</h2>
			<ul>
			<% @accounting_imputation.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>
	<table class="data" style="width:100%" >
		<tr>
			<td style="width:30%;background-color: #C3D5D5;"><strong><%= f.label :detalle %></strong></td>
			<td style="width:70%">
				<%= f.text_field :detalle %>
			</td>
		</tr>
	</table>
	<p><%= f.submit %></p>
<% end %>
