= form_for(@bank_deposit_account_type) do |f|
  - if @bank_deposit_account_type.errors.any?
    #error_explanation
      %h2
        = pluralize(@bank_deposit_account_type.errors.count, "error")
        prohibited this bank_deposit_account_type from being saved:
      %ul
        - @bank_deposit_account_type.errors.full_messages.each do |msg|
          %li= msg
  .clear
  %br/
  .form_box
    .form_box_title
      %span.title= BankDepositAccountType.model_name.human
    %table.tableForm
      %tr
        %td.columnLabel
          = f.label :detalle, :class => "apsLabel"
        %td.columnData
          = f.text_field :detalle, :class => "apsTextbox"
  .actions
    = f.submit :class => "btn success"
