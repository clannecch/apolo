<h1>Listing employments</h1>

<table>
  <tr>
    <th>Detalle</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employments.each do |employment| %>
  <tr>
    <td><%= employment.detalle %></td>
    <td><%= link_to 'Visualizar', employment %></td>
    <td><%= link_to 'Editar', edit_employment_path(employment) %></td>
    <td><%= link_to 'Borrar', employment, :confirm => 'Esta Seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Ocupacion', new_employment_path %>
