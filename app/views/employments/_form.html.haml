= form_for(@employment) do |f|
  - if @employment.errors.any?
    #error_explanation
      %h2
        = pluralize(@employment.errors.count, "error")
        prohibited this employment from being saved:
      %ul
        - @employment.errors.full_messages.each do |msg|
          %li= msg
  .clear
  %br/
  .form_box
    .form_box_title
      %span.title= Employment.model_name.human
    %table.tableForm
      %tr
        %td.columnLabel
          = f.label :detalle, :class => "apsLabel"
        %td.columnData
          = f.text_field :detalle, :class => "apsTextbox"
  .actions
    = f.submit :class => "btn success"
