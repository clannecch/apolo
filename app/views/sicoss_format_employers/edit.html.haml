
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossFormatEmployer.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_format_employer
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_format_employers_path

