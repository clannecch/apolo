
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossEmployerType.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_employer_type
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_employer_types_path

