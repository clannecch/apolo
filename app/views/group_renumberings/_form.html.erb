<%= form_for(@group_renumbering) do |f| %>
  <% if @group_renumbering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_renumbering.errors.count, "error") %> prohibited this group_renumbering from being saved:</h2>

      <ul>
      <% @group_renumbering.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :detalle %><br />
    <%= f.text_field :detalle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
