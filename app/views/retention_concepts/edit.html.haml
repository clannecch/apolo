- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item RetentionConcept.model_name.human, retention_concepts_path 
    - breadcrumb.item "#{@retention_concept.detalle.titlecase}", retention_concept_path(@retention_concept), :active => true

= link_to t('scaffold.actions.show').titlecase, retention_concept_path(@retention_concept), :class => "btn info" 
.clear
%br/
= render 'form'
