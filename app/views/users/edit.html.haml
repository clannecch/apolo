- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => User.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @user
\|
= link_to t('scaffold.actions.back').titlecase, users_path

