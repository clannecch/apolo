- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item GroupEmployerContribution.model_name.human, group_employer_contributions_path
    - breadcrumb.item t('scaffold.actions.new', :item => GroupRemuneration.model_name.human).titlecase , group_employer_contributions_path, :active => true

= render 'form'
