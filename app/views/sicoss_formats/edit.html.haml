
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossFormat.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_format
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_formats_path

