
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossContractMode.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_contract_mode
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_contract_modes_path

