- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item GroupRetention.model_name.human, group_retentions_path
    - breadcrumb.item "#{@group_retention.detalle.titlecase}", group_retentions_path(@group_retention), :active => true

= link_to t('scaffold.actions.show').titlecase, group_retention_path(@group_retention), :class => "btn info"
.clear
%br/
= render 'form'
