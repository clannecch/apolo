
- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => SicossDamaged.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @sicoss_damaged
\|
= link_to t('scaffold.actions.back').titlecase, sicoss_damageds_path

